# Open Infrastructure Blockers

**Verified snapshot:** 2026-07-10 (Pass 3)

Completed DNS/provider work is not listed as a blocker: authenticated GoDaddy control is confirmed for all ten domains; Explore&Earn Resend DNS/provider verification is complete; and 15 Clerk CNAMEs resolve publicly. Remaining gates are below.

| Priority | Venture(s) | Remaining blocker | Classification | Exact owner action |
|---|---|---|---|---|
| P0 | Explore&Earn, Sweepza | Mixed live Stripe account: two customers, one draft invoice, mixed webhooks/account identity | **requires founder decision** and **blocked by production risk** | Select separation option A/B/C, legal retaining owner, and disposition of both customers/draft invoice. Written approval must separately authorize live mutation; no live state has changed |
| P0 | Explore&Earn | Dark production Clerk DNS is Verified and SSL Issued, but OAuth/JWT/webhook/admin/credentials/runtime proof are incomplete | **blocked by production risk** | Configure provider settings; install credentials only in dark Preview and prove signup/login/profile/webhook/admin flows before `prd` |
| P0 | Sweepza | Dark production Clerk DNS is Verified/SSL Issued, but live `prd` still uses dev instance | **blocked by production risk** | Configure production settings, prove Preview, then replace `prd`; decide staging isolation and dev-user re-registration |
| P0 | LogLoads | Dark production Clerk DNS is Verified/SSL Issued, but hosting/source decision and runtime configuration are unresolved | **blocked by production risk** and **requires founder decision** | Choose persistent single-writer host (recommended) or complete database-canonical/stateless Vercel refactor; then configure/prove production Clerk |
| P0 | Explore&Earn | Resend domain is verified, but current key is broad; explicit sender, Doppler/runtime sync, and delivery smoke are absent | **blocked by production risk** | Create sending-only/domain-restricted replacement, install through Doppler, deploy/smoke custom-domain delivery, then retire broad key only after zero use |
| P0 | Sweepza, LogLoads | Team is at 1/1 domain. Sweepza has real mail consumers but no key/from; LogLoads has a broad same-team key but no domain/from | **blocked by payment/plan** and **requires founder decision** | Approve independent team/account cost and recovery owners per mail-sending venture. Until then deploy truthful `skipped` behavior for Sweepza and keep LogLoads mail inactive |
| P0 | ORAN | Local remediation `5ec3561…` passes all checks but is unpushed; no Preview credentials/runtime vars; 53 repository migrations vs one managed ledger entry; no functional rollback | **blocked by production risk** | Review/push local commit, safely install scoped Preview authority/config, reconcile/backup database ledger, run exact-SHA Preview, create a known-good candidate/rollback, then separately approve DNS |
| P0 | Lake & Pine | Local validated commit `a55ccff…` is unpushed/no PR/Preview; two dirty `122dd312` artifacts are not exact builds | **blocked by production risk** | Obtain approved GitHub auth, push/review PR, prove exact-SHA Preview and clean `main`; retain `dpl_AaAE…` as temporary rollback |
| P1 | Explore&Earn, BidSpace, LogLoads | One unrestricted default Mapbox public token; branch-qualified consumers differ | Credential creation **awaits explicit founder approval**; replacement/deployment is **blocked by production risk**; BidSpace also **blocked by missing domain** | Create the approved restricted public tokens only for proven consumers, using `styles:read`/`fonts:read` and exact venture origins; test fallbacks and unrelated-origin rejection before retiring the default token |
| P1 | Explore&Earn, ORAN | Supabase migration ledgers do not explain deployed schemas | **blocked by production risk** | Approve backup/schema-diff/reconciliation pass. ORAN specifically has 53 repo migrations versus one managed entry |
| P1 | AutomatedEmpires | Draft PR #6 is open/mergeable at `cc8000a…` with Vercel success, but no clean `main` production artifact exists | **blocked by production risk** | Review/merge PR #6, verify `main` provider deployment and rollback, then plan reversible `automatedempires.com` web cutover |
| P1 | BidSpace | No final domain | **blocked by missing domain** and **requires founder decision** | Select and acquire/identify the final owned domain before Clerk/Mapbox/mail production resources |
| P1 | ORAN | Domain auto-renew is off | **requires founder decision** | Confirm renewal intent and billing owner; do not allow expiry during migration |
| P1 | Explore&Earn | Five GitHub repository secrets remain outside Doppler | **blocked by production risk** | Replace each workflow consumer with a tested authoritative credential before deleting any write-only secret |
| P2 | Sweepza | Only E&E has a live PostHog project; Sweepza key is empty. Sweepza Sentry project exists but has no first event/owner routing | **blocked by production risk** | Reauthorize PostHog management, create a separate project if approved, configure scoped runtime values, and prove ingestion; configure venture Sentry team/owner/alert routing and first event |
| P2 | Sentry ventures | Four distinct projects exist, but one owner-grade token in E&E `dev` spans the org; alerts are unowned and IP scrubbing is off | **requires founder decision** and credential-rotation gate | Choose venture teams/owners/routing/scrubbing policy; create narrow read/build credentials, install and verify them, then retire owner-grade runtime access |
| P2 | Explore&Earn / future media ventures | One Free Cloudinary product environment, 1,165 objects, 35 outside E&E namespace; signed presets unbound with overwrite enabled | **blocked by payment/plan** and **requires founder decision** | Choose shared-E&E designation, paid product environments, or standalone account. Clone safer environment-specific presets first; do not migrate media without an approved checksum/URL/rollback plan |

## Deferred deletion review

Do not delete Vercel projects `explore-and-earn-vercel-fix` or `sweepza-canary`, or Doppler project `example-project`, until aliases, traffic, rollback, CI, domain, billing, and ownership dependencies are disproven and deletion is explicitly approved.
