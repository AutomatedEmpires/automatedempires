# Open Infrastructure Blockers

**Verified snapshot:** 2026-07-12 (Pass 5 Mapbox/PostHog refresh)

Completed DNS/provider work is not listed as a blocker: authenticated GoDaddy control is confirmed for all ten domains; Explore&Earn Resend DNS/provider verification is complete; and 15 Clerk CNAMEs resolve publicly. Remaining gates are below.

| Priority | Venture(s) | Remaining blocker | Classification | Exact owner action |
|---|---|---|---|---|
| P0 | Explore&Earn | Dedicated live/test Stripe accounts and the canonical catalogs now exist, but the deployed production credential has not been safely proven to resolve to the intended live account and the correct live webhook is absent | **blocked by production risk** | Prove only the account ID through a safe runtime check, create/verify the venture webhook, pass exact-SHA Preview, then promote the verified binding without exposing or prematurely retiring credentials |
| P0 | Sweepza | Dedicated live/sandbox accounts and price bindings are aligned, but the live account retains a foreign Explore&Earn webhook plus two unclassified customers and one $0 draft invoice | **blocked by production risk**; destructive object cleanup requires explicit approval | Keep the foreign endpoint until Explore&Earn's replacement is verified. Classify the three non-money objects using non-sensitive metadata; do not delete or mutate them without approval |
| P0 | Explore&Earn | Dark production Clerk DNS is Verified and SSL Issued, but OAuth/JWT/webhook/admin/credentials/runtime proof are incomplete | **blocked by production risk** | Configure provider settings; install credentials only in dark Preview and prove signup/login/profile/webhook/admin flows before `prd` |
| P0 | Sweepza | Dark production Clerk DNS is Verified/SSL Issued, but live `prd` still uses dev instance | **blocked by production risk** | Configure production settings, prove Preview, then replace `prd`; decide staging isolation and dev-user re-registration |
| P0 | LogLoads | Supabase-canonical PR #6 final source `f280ef4fef4b992f94457aad61cfe27e8ec91791` is merged to default `9c9e107082942e5bce782eac2ce71aa63eb7d9c0` and clean main production `dpl_Xxr…8dPtF` is `READY`, but live migration, backup/live upgrade, environment provenance, distributed rate limiting, provider activation, and functional rollback remain | **blocked by production risk** | Preserve source/deployment provenance, then prove live-shape upgrade, controls, and functional rollback before live migration, Clerk/provider activation, or DNS |
| P0 | Explore&Earn | Scoped Resend credential/identities/lanes and merged green PR #243 are complete; exact-source Vercel status passed, but a real delivery smoke is not authorized/proven | **blocked by production risk** | Obtain the separate delivery authorization, run a controlled custom-domain/reply smoke, inspect provider evidence, then retire the broad key only after zero use |
| P0 | Sweepza, LogLoads | Team is at 1/1 domain. Sweepza has real mail consumers but no key/from; LogLoads has a broad same-team key but no sending domain | **blocked by payment/plan** under the explicit no-upgrade decision | Deploy truthful `skipped` behavior for Sweepza, keep LogLoads transactional mail inactive, and do not reuse Explore&Earn's sender identity |
| P0 | ORAN | Required checks are green on draft PR #58 head `6d5caf226bb4940c6bc0f270f77c43e3cb30b1da`, but Codecov patch, Visual Regression, and runbook freshness fail; no safe Preview data/credential lane, exact-SHA deployment, reconciled schema authority, or functional rollback exists | **blocked by production risk** | Keep the PR draft; repair ancillary gates, establish narrowly scoped Preview authority without replaying unsafe migrations, reconcile/backup schema authority, then prove Preview and rollback before merge or DNS |
| P0 | Lake & Pine | PR #1 merged as `1b6a877bc054a9239c2a430aaf668996de8a0302` and clean merged-main production `dpl_EHG…MDWVk` is `READY`, but functional rollback/DNS smoke remains | **blocked by production risk** only for domain cutover | Prove the provider-hostname smoke and functional rollback, export the zone, then change only approved web records; preserve mail/DMARC |
| P1 | Explore&Earn, BidSpace, LogLoads, Lake & Pine | Proven browser consumers need one public token per approved app across `dev`/`stg`/`prd`. Every founder-created named token inspected in Pass 5 is a secret `sk` token, unrestricted and at zero usage, so none can enter `NEXT_PUBLIC_MAPBOX_TOKEN`. The old shared public token remains rollback in previously audited lanes | **blocked by security / secure public-token handoff** and downstream **production risk**; BidSpace production restriction also **blocked by missing domain**; Lake & Pine **requires founder decision** | Create minimum-scope public `pk` replacements with actual origins for Explore&Earn, LogLoads, and BidSpace; decide Lake & Pine separately; have the founder place approved tokens directly in matching Doppler configs, sync only matching Vercel projects, verify allowed/rejected origins and fallbacks, then retire the shared token only after zero use |
| P1 | Explore&Earn, ORAN | Supabase migration ledgers do not explain deployed schemas | **blocked by production risk** | Run backup/read-only schema diff and produce an exact reconciliation plan. ORAN specifically has 53 repo migrations versus one managed entry; do not replay migrations blindly |
| P1 | AutomatedEmpires | Default `664cda67bc7bc91e4f1577d76bbe7758290f3a2d` has clean merged-main production `dpl_4b8…k5bT9` `READY`, but functional rollback/DNS smoke is incomplete | **blocked by production risk** only for web cutover | Verify provider-hostname smoke and functional rollback, then plan reversible Website Builder replacement |
| P1 | BidSpace | Final custom domain purchase is explicitly deferred | **blocked by missing domain** only for production custom-domain/email work | Continue GitHub, Doppler, Vercel Preview, Supabase, Clerk dev/stg, sandbox payments, Sentry, Cloudinary namespace, and map-dev work without inventing a production domain |
| P1 | Explore&Earn | Five GitHub repository secrets remain outside Doppler | **blocked by production risk** | Replace each workflow consumer with a tested authoritative credential before deleting any write-only secret |
| P2 | AutomatedEmpires, Explore&Earn, Sweepza, LogLoads, BidSpace, Lake & Pine | Separate PostHog projects and own key/host pairs are presence-verified in Doppler all lanes and matching Vercel Development/Preview/Production targets. Ops draft PR #8 auto-triggered an AutomatedEmpires Preview, but no controlled runtime event/ingestion proof or production deployment exists; the other five have no post-write deployment | Provider/environment configuration **completed**; activation **blocked by controlled deployment/event and production-risk proof** | Fresh-deploy the exact consuming SHA per venture as appropriate, send one controlled non-sensitive event, verify the expected project/environment/health, then record runtime activation |
| P2 | ORAN | PostHog remains intentionally absent because ORAN is civic infrastructure without a stable production deployment or approved analytics/consent contract | **requires founder/privacy decision** | Prefer Sentry/logs. Revisit only after privacy-safe event taxonomy, consent, retention, and deployment posture are approved |
| P2 | Sentry ventures | Seven distinct projects, privacy scrubbers/IP scrubbing, ownership routing, one alert/project, issue assignment, and new-project Doppler bindings are complete. Vercel bindings/runtime events and owner-grade build-token replacement remain | **blocked by production risk / Vercel env-write authority** | Install each venture binding without display, deploy and verify controlled events/releases, then replace the broad token through create → install → deploy → verify → revoke |
| P2 | Explore&Earn / future media ventures | The Free environment now has all nine required top-level namespaces, but remains one shared administrative boundary. Fifty-five former E&E icon raws are not runtime icon refs and still need manifest/docs classification; active presets remain unsafe to change without proof | Transfer separation **blocked by payment/plan**; media/preset mutation **blocked by production risk** | Preserve product media, finish reference/export classification, clone safer presets only through disposable Preview fixtures, and do not move/delete assets without checksum/URL/rollback evidence |

## Deferred deletion review

Do not delete Vercel projects `explore-and-earn-vercel-fix` or `sweepza-canary`, or Doppler project `example-project`, until aliases, traffic, rollback, CI, domain, billing, and ownership dependencies are disproven and deletion is explicitly approved.
