# Open Infrastructure Blockers

**Verified snapshot:** 2026-07-10 (Pass 2)

These are the remaining gates after all safe, non-destructive work available in the authenticated session. No blocker below was bypassed by guessing, purchasing, deleting, or changing production traffic.

| Priority | Venture(s) | Blocker | Why execution stopped | Exact owner action |
|---|---|---|---|---|
| P0 | Explore&Earn, Sweepza | Shared live Stripe account, mixed webhooks/identity, two customers, and one draft invoice | The written plan is complete, but choosing the legal retaining venture and mutating live customers/invoice/account state requires founder approval | Select option A, B, or C in `STRIPE_LIVE_ACCOUNT_SEPARATION_PLAN.md`; assign legal/payout/tax ownership and customer/draft-invoice disposition; separately approve replacement activation and live cutover |
| P0 | Explore&Earn, LogLoads, Sweepza | Resend team/domain separation and verified sending are missing | Explore&Earn domain is failed; LogLoads shares the team through a broad key; Sweepza is unconfigured. A separate team or Pro capacity is a recurring-cost decision and DNS is not authenticated | First authorize authenticated DNS work for Explore&Earn. Then approve a separate paid LogLoads team (recommended) or an explicit shared-plan exception; decide whether Sweepza email is required and fund its separate team |
| P0 | Explore&Earn, Sweepza, LogLoads | Production Clerk separation is missing | Production needs authenticated dashboard/DNS, OAuth, webhook, and application smoke tests. The one E&E and one Sweepza dev users cannot be transferred to production | Install/enable the ChatGPT Chrome Extension, activate dark venture production instances, configure DNS/OAuth/webhooks, and approve re-registration/cutover only after preview evidence |
| P0 | ORAN | Public site target is broken and is not a functional rollback; the Vercel convergence branch is unmerged and unverified | Restoring Azure A record `20.115.232.12` would restore a 404/invalid-TLS target, so DNS cutover currently has no functional recovery target | Repair CI/env/CSP/schema gaps, produce exact-SHA preview and production-candidate artifacts, and establish a functioning Vercel or repaired Azure rollback before approving DNS |
| P0 | LogLoads | Hosting architecture is undecided; current source requires one persistent-volume writer while Vercel is multi-instance/serverless | Fixing the `.pnpm`/ENOENT packaging issue would not make process-local authoritative state safe; dirty feature/main histories also diverge | Choose Path A (single-node persistent-volume host) or Path B (make Supabase/Postgres canonical and prove stateless concurrency), then reconcile source and deploy cleanly |
| P0 | Lake & Pine | READY alias is an unreproducible dirty redeploy; `main` lacks `apps/web` | Candidate `e1fe8f0` is five commits ahead and adds the application, but a later automatic main deploy could replace the recovery artifact | Review/merge or reproduce exact candidate `e1fe8f0`, add CI, prove an exact-SHA preview/main candidate, and retain deployment `dpl_AaAEZZz9Ma442TDNygN6ypLsQCMt` for rollback |
| P1 | Explore&Earn, ORAN | Supabase remote migration ledgers do not cleanly explain deployed schemas | Ledger repair is a destructive/high-risk database governance action | Approve a dedicated migration-reconciliation pass with backups and schema diffs |
| P1 | BidSpace | No final production domain is known | Domain naming/purchase is a founder and payment decision | Select the final name and purchase or identify the owned domain |
| P1 | Lake & Pine | lakepinecleaning.com is unregistered | Domain purchase requires payment and ownership choice | Purchase the chosen domain or select a replacement |
| P1 | AutomatedEmpires, LogLoads | Domains still route to GoDaddy Website Builder | The parent has only a protected preview; LogLoads has no approved production architecture. No authenticated zone export or functional rollback exists | Cut over only after each chosen runtime passes and the existing zone/rollback is recorded |
| P1 | All registered domains | Authenticated registrar control and full DNS-zone export remain unavailable | Chrome is installed but not running; the selected profile lacks the ChatGPT Chrome Extension. The native host is healthy. Public DNS proves routing, not account authority | Approve launching Chrome and opening the extension page, install/enable it, authenticate GoDaddy, then permit the non-destructive ownership/zone review |
| P1 | Explore&Earn, BidSpace, LogLoads | Shared apparently unrestricted Mapbox public token; LogLoads mislabels it as a server token; Explore&Earn server token is revoked | Current authorization lacks `tokens:read`/`tokens:write`, so replacements cannot be created or safely inventoried | Use account-owner dashboard access to create scoped E&E tokens first; wait for BidSpace domain and LogLoads runtime/consumer; deploy one venture at a time before retiring shared token |
| P1 | Explore&Earn | Five GitHub repository secrets remain outside Doppler | GitHub values are write-only; removal before tested replacement could break CI | Authorize replacement tokens/password reset where needed, then migrate workflows to Doppler and remove confirmed-unused secrets |
| P2 | Sweepza | Health reports missing Clerk webhook, PostHog, Sentry, and Notion configuration | Missing resources/settings require provider-specific product decisions and credentials | Decide which integrations are production requirements, then provision separate projects/keys and test each endpoint |
| P2 | Explore&Earn / future media ventures | Cloudinary has one Free product environment; only Explore&Earn has assets/credentials, while its signed presets are not folder-bound and allow overwrite | Plus begins at $99/month; separate accounts require owner setup; migration would change hardcoded delivery URLs. No current cross-venture asset migration is needed | Choose temporary shared, paid parent environments, or standalone venture accounts. For transfer-grade ownership, approve a standalone Explore&Earn destination before any checksum-tested migration |
| P2 | Multiple ventures | PostHog, Sentry, and some provider ownership/retention settings could not be dashboard-verified | No management credentials or active Chrome extension session were available | Provide authenticated dashboard access in a follow-up pass and confirm billing owners, retention, alerts, and data residency |

## Deferred deletion review

Do not delete the Vercel projects explore-and-earn-vercel-fix or sweepza-canary, or the Doppler project example-project, until a human confirms that each is unused and no rollback, CI, domain, or billing dependency remains.
