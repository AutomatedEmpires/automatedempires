# Resend Domain Matrix

**Verified provider snapshot:** 2026-07-10
**Pass 2 status:** Refreshed through read-only Resend API calls and environment mapping. Billing-plan identity is not exposed by the API.
**Pass 3 authenticated status:** Authenticated dashboard control confirmed for the Explore&Earn team/account. It contains one domain, `exploreandearn.com`, in `us-east-1`; sending is enabled and receiving is disabled. The domain was `failed` before remediation. Its three provider-issued DNS records now exist in GoDaddy, resolve publicly, and Resend reports the domain verified. A scoped replacement API key, runtime synchronization, and delivery smoke test have not yet been completed.
**Pass 4 execution status:** A sending-only, `exploreandearn.com`-restricted key was transferred without display into Doppler `dev`/`stg`/`prd` and Vercel Development/Preview/Production as restricted/sensitive. An earlier temporary replacement was revoked. The broad key remains rollback. From is `notifications@exploreandearn.com`; Reply-To is `support@exploreandearn.com`. PR #243 passed CI, CodeQL, Migration Guard, and Vercel and merged at `b616b9e10fa434422dd34442f6cb24194cf8d5ec`; clean production `dpl_5HCPaCNCQuyip2iZTHpoMvSxjQFY` is `READY`. Real delivery is not authorized/proven.
**Security note:** API values, DKIM material, SMTP credentials, and credential fingerprints are intentionally excluded.

A distinct API key does not create an independent account or sending-domain boundary. DNS verification must be established from current provider-issued records and authoritative DNS, not inferred from source code.

## Current venture boundaries

| Venture | Current account/domain evidence | Key separation | Contamination or blocker | Pass 4 classification | Safe next action |
|---|---|---|---|---|---|
| AutomatedEmpires | No AutomatedEmpires-specific Resend domain was verified | Not configured | No transactional-mail consumer or contamination confirmed | **completed no-resource decision** for the current parent site | Keep absent until a reviewed transactional consumer exists; `jackson@automatedempires.com` remains an admin mailbox, not a Resend requirement. |
| Explore&Earn | The sole verified sending domain remains `exploreandearn.com` in `us-east-1`; DKIM/sending MX/SPF resolve and sending is enabled | Sending-only, domain-restricted key is installed as restricted/sensitive in all approved Doppler/Vercel lanes. Broad key retained only as rollback | Active runtime boundary is scoped; From/Reply-To explicit; merged source and clean `READY` production provenance verified. Real delivery is not authorized/proven. Legacy LogLoads same-team authority remains | Credential/source/environment remediation **safely fixed now**; delivery and broad-key retirement **blocked by production risk** | Obtain separate delivery authorization; send a controlled support-mailbox smoke; verify reply routing, provider authentication/logging, and zero broad-key use before revocation. |
| ORAN | No ORAN Resend resource; existing Mailgun DNS is preserved | Not configured | No Resend consumer proven; changing provider could disturb mail | **completed no-Resend decision** for the current migration | Preserve Mailgun until source/runtime evidence intentionally selects another provider; do not provision speculatively. |
| BidSpace | No owned production domain; purchase explicitly deferred | Not applicable | Production sender cannot be selected safely | **blocked by missing domain** for production | Do not invent a domain or create placeholder production mail resources; dev/Preview provider setup may proceed without email. |
| Lake & Pine | Owned `lakeandpinecleaning.com`; no Resend resource, MX, or root SPF | Not configured | Clean runtime/transactional consumer not established | **blocked by production risk** until clean deployment proves a mail consumer | Preserve current web/DMARC records. Configure only after source/runtime needs it; do not consume the unused paid mailbox allocation implicitly. |
| Sweepza | No account/domain/key/from in any lane; product code calls mail for approval/hold and winner publication | Not configured | Calls must no-op truthfully; source fix is merged through #47 and retained in current `main` `4c0aad183fe9442e4546985b373b26498e38e6e7`; production `dpl_9N57qj7PHDteARUpVFWCKAxYutts` is `READY` | Truthful source/deployment provenance **fixed and verified**; provider setup **blocked by payment/plan** under the explicit no-upgrade decision | Verify `skipped`/null `sent_at` through a controlled runtime flow. Keep transactional mail disabled and preserve Microsoft 365 MX. |
| LogLoads | `support@logloads.com` is a real mailbox, but no LogLoads transactional-sending domain exists in Resend. A distinct legacy broad key still reaches the Explore&Earn team; a prior domain create returned HTTP `403` at the current quota | The key differs from Explore&Earn's values but remains unacceptable account-level authority. No production sender identity or delivery proof exists | **Confirmed account-level contamination and current-plan/domain-quota blocker.** Mailbox availability does not make Resend sending ready | **blocked by payment/plan**; transactional sending intentionally deferred | Keep product support/reply identity configured where code permits, but remove or disable the legacy team credential only through a verified deployment-safe change. Do not send from Explore&Earn. Provision an independent team/domain only after paid approval. |

## Safe remediation sequence

1. Treat the LogLoads domain-capacity error as a current-plan quota and purchase gate; do not retry around it. The observed one-domain behavior is consistent with Free, but the billing dashboard must confirm the actual plan.
2. Prefer a separate LogLoads team/account for independent transfer. An approved upgrade can increase domain capacity but does not create account ownership separation.
3. Treat Explore&Earn DNS, scoped key/environment, merged source, and exact-source Preview as completed. Do not declare sending ready until a separately authorized custom-domain delivery/reply smoke passes.
4. Add only provider-issued DNS records, preserve existing MX records, and verify SPF, DKIM, DMARC, and a real delivery independently.
5. Replace runtime full-access keys with sending-only, domain-restricted keys. Deploy and verify replacements before removing current keys.

At this snapshot, published pricing lists one domain on Free and ten domains on Pro. A Pro upgrade is an explicit payment/plan gate; a separate team/account remains the cleaner transfer boundary.

Pass 3 changed only Explore&Earn DNS/provider verification. Pass 4 added the scoped credential, direct Doppler/Vercel installation, explicit sender/reply identity, and green source changes. No delivery smoke, broad-key retirement, paid upgrade, LogLoads/Sweepza transactional activation, or mail-account migration is claimed.
