# Resend Domain Matrix

**Verified provider snapshot:** 2026-07-10
**Pass 2 status:** Refreshed through read-only Resend API calls and environment mapping. Billing-plan identity is not exposed by the API.
**Pass 3 authenticated status:** Authenticated dashboard control confirmed for the Explore&Earn team/account. It contains one domain, `exploreandearn.com`, in `us-east-1`; sending is enabled and receiving is disabled. The domain was `failed` before remediation. Its three provider-issued DNS records now exist in GoDaddy, resolve publicly, and Resend reports the domain verified. A scoped replacement API key, runtime synchronization, and delivery smoke test have not yet been completed.
**Security note:** API values, DKIM material, SMTP credentials, and credential fingerprints are intentionally excluded.

A distinct API key does not create an independent account or sending-domain boundary. DNS verification must be established from current provider-issued records and authoritative DNS, not inferred from source code.

## Current venture boundaries

| Venture | Current account/domain evidence | Key separation | Contamination or blocker | Pass 3 classification | Safe next action |
|---|---|---|---|---|---|
| AutomatedEmpires | No AutomatedEmpires-specific Resend domain was verified | Unknown | No contamination confirmed | **requires founder decision** — no parent transactional-mail use case is approved | Create a sending identity only for a defined parent-company mail use case; verified DNS control now exists. |
| Explore&Earn | The sole domain in the authenticated Resend team/account is `exploreandearn.com` in `us-east-1`. It was **`failed`** before remediation, with one DKIM and two sending-authentication checks failed. Sending is enabled and receiving is disabled. All three provider-issued records now exist, resolve publicly, and the domain is provider-verified | **Confirmed distinct key from LogLoads**. Both existing keys can list administrative resources, so both are broad/full-access rather than sending-only | **Confirmed account-level co-tenancy with LogLoads**; key duplication was not found. `RESEND_FROM_EMAIL` is absent. DNS/provider-domain verification is complete, but no scoped replacement key has been created, synchronized to runtime, deployed, or smoke-tested | **safely fixed now** — DNS and provider-domain verification are completed; runtime credential separation and delivery proof remain **blocked by production risk** | Create a sending-only, domain-restricted replacement key; synchronize only the approved Explore&Earn runtime environments; deploy and prove custom-domain delivery. Retire the broad key only after verification. Do not authorize LogLoads production mail from this identity. |
| ORAN | No ORAN Resend domain/account resource was verified | Unknown | Existing Mailgun routing must not be disturbed without a mail-provider decision | **requires founder decision** | Reconcile the intended mail provider before adding Resend; preserve existing mailbox and transactional-mail records. |
| BidSpace | No final owned domain was verified, so a production sending domain cannot be selected safely | Not applicable | Domain decision and ownership are absent | **blocked by missing domain** and **requires founder decision** | Do not invent a domain or create placeholder production mail resources. |
| Lake & Pine | Authenticated ownership and the complete eight-record zone for `lakeandpinecleaning.com` are confirmed; no Resend resource, MX, or root SPF exists | Not configured | The Pass 2 missing-domain assertion is resolved, but runtime and transactional-mail intent are not | **requires founder decision**; production use is **blocked by production risk** until the clean deployment plan passes | Decide whether transactional email is required only after clean deployment provenance exists; preserve the current web and DMARC records during any future setup. |
| Sweepza | No Sweepza-specific Resend account/domain exists, and no key/from-address is present in `dev`, `stg`, or `prd`; however, production code calls Resend for listing approval/hold and winner publication | Not configured | Calls currently no-op, while pre-remediation code falsely records them as `sent`. Local commit `89bbe121…` corrects this to `skipped`/null `sent_at` but is not pushed or deployed | Email is a **verified product dependency**; safe truthfulness fix is local only. Provider setup is **blocked by payment/plan** and **requires founder decision** | Review/push/deploy the truthful-result patch. Approve an independent team and scoped sending subdomain while preserving Microsoft 365 MX; install through `stg` and prove delivery before production. |
| LogLoads | A **separate LogLoads API key** exists in the same team used by Explore&Earn. `dev` and `prd` reuse that LogLoads key; there is no LogLoads domain. A prior create attempt returned HTTP `403` at the current domain quota | Key differs from Explore&Earn, but it is broad/full-access and account capacity is shared. `LOGLOADS_EMAIL_FROM` is absent | **Confirmed account-level contamination and current-plan/domain-quota blocker**; code otherwise falls back to the provider's testing sender | **blocked by payment/plan** and **requires founder decision** on a separate account versus plan upgrade | Prefer a separate LogLoads team/account for transferability. If the founder instead approves a plan upgrade, create and verify a LogLoads-owned domain, replace the broad key with a sending-only domain-restricted key, and configure an explicit from-address before production use. |

## Safe remediation sequence

1. Treat the LogLoads domain-capacity error as a current-plan quota and purchase gate; do not retry around it. The observed one-domain behavior is consistent with Free, but the billing dashboard must confirm the actual plan.
2. Prefer a separate LogLoads team/account for independent transfer. An approved upgrade can increase domain capacity but does not create account ownership separation.
3. Treat Explore&Earn DNS and provider-domain verification as completed, but do not declare its runtime sending path ready until a sending-only/domain-restricted key is synchronized, deployed, and proven by a custom-domain delivery smoke test. Create LogLoads or Sweepza domains only after their independent account/payment boundaries are approved.
4. Add only provider-issued DNS records, preserve existing MX records, and verify SPF, DKIM, DMARC, and a real delivery independently.
5. Replace runtime full-access keys with sending-only, domain-restricted keys. Deploy and verify replacements before removing current keys.

At this snapshot, published pricing lists one domain on Free and ten domains on Pro. A Pro upgrade is a paid founder gate; a separate team/account remains the cleaner transfer boundary.

Pass 3 changed only Explore&Earn DNS/provider verification in Resend. No Resend account, API key, runtime environment, or mail state was migrated. The separately prepared Sweepza code fix has not been pushed or deployed. No scoped replacement key has been created or installed, no production key has been revoked, and no post-remediation delivery smoke test is claimed.
