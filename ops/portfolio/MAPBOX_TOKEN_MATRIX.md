# Mapbox Token Matrix

**Verified snapshot:** 2026-07-10 (Pass 3 branch-qualified consumer refresh)
**Execution state:** Consumer, environment-name, equality, and authenticated dashboard checks completed. No token was created, changed, installed, printed, or revoked.

The authenticated account contains exactly one default public token. It is unrestricted, and Mapbox does not permit URL restrictions on the default token. Dashboard token-write control is available; credential creation is paused only for explicit action-time founder approval. The current public value is shared by Explore&Earn, BidSpace, and LogLoads configuration and remains active as rollback.

| Venture / exact source | Runtime consumer | Current configuration | Separation state | Pass 3 classification | Safe next action |
|---|---|---|---|---|---|
| Explore&Earn `main` `528d28ccfb36…` | **Yes.** `apps/web/components/map/MapView.tsx` uses Mapbox GL JS with `NEXT_PUBLIC_MAPBOX_TOKEN` for the `/map` surface | Public token configured only in `dev`; separate server-shaped token is revoked/invalid and no server consumer was found | Public token is shared; `stg`/`prd` values are not established in Doppler | **awaiting explicit approval** for dev/prd credentials; staging **intentionally deferred** | Create non-default public dev and production tokens with only `styles:read` and `fonts:read`. Restrict dev to `http://localhost:3000` and production to `https://exploreandearn.com`. Choose one canonical staging hostname before creating staging; do not replace the revoked server token. |
| BidSpace `main` `0b20189658f2…` | **No current runtime consumer.** Source contains planning/config placeholders; map UI remains pending | Shared public token appears in `dev`; no final product domain | Unused shared configuration is not a production boundary | **blocked by missing domain**; safe deprovision review | Do not provision a production token yet. Remove the unused value only after a clean build proves no hidden consumer; create a BidSpace token when the map UI and owned domain exist. |
| BidSpace accepted feature `511b763…` | **Yes.** The accepted recovered product branch uses Mapbox GL JS with the public token and `light-v11` | Feature source and environment name exist, but no owned production domain exists | Active feature consumer would remain coupled if promoted | **blocked by missing domain** and clean-source review | Keep the shared token as rollback; choose the venture domain and reviewed source before creating restricted dev/production tokens. |
| LogLoads `main` `e78b48c…` | No map application on `main` | Shared public value is stored in both public and server-shaped names | Mislabelled public value; `main` is not the deployed feature candidate | **blocked by production risk** | Do not rotate from `main` evidence alone. Reconcile the feature branch and architecture first. |
| LogLoads feature `cce1c449…` | **Yes, optional.** `apps/web/components/v3/RealMap.tsx` uses Mapbox GL JS when `NEXT_PUBLIC_MAPBOX_TOKEN` exists and otherwise uses the keyless MapLibre/Carto fallback | `dev`/`prd` carry the shared public value; the server-shaped name is unused and misleading | Public-token coupling is real, but the application has a safe no-Mapbox fallback | Dev token **awaiting explicit approval**; production **intentionally deferred** pending hosting/source decision | Create only `logloads-dev-public`, restricted to `http://localhost:3002`, after approval. Keep MapLibre fallback active. Create a production token for `https://logloads.com` only after source/architecture convergence; remove the unused server-shaped name after clean-build proof. |
| Lake & Pine candidate `e1fe8f00…` | **Yes, optional.** `AreaMap.tsx` uses Mapbox GL JS and falls back to a branded SVG when the public token is absent | No active token was found in the current Lake & Pine Doppler map | No cross-venture credential reuse confirmed | **intentionally deferred** until a clean exact-SHA preview | After candidate fixes are committed and a clean preview exists, create a Lake & Pine public token for `https://lakeandpinecleaning.com`; use a separate dev token for localhost. Do not borrow the portfolio token. |
| Sweepza | No map consumer; product notes explicitly say none today | No active token verified | No contamination confirmed | **completed** no-resource decision | Do not provision a token unless a reviewed geospatial feature is implemented. |
| AutomatedEmpires / ORAN | No Mapbox consumer verified; ORAN uses Leaflet/OpenStreetMap in the migration candidate | No active token verified | No contamination confirmed | **completed** no-resource decision | Do not copy the shared token. |

## Token rules

- Browser Mapbox GL JS consumers use non-default public tokens with `styles:read` and `fonts:read`. Do not grant management or write scopes.
- Mapbox URL restrictions do **not** support wildcard characters or IP addresses. A restriction containing only the HTTPS apex domain also authorizes its subdomains and paths; specifying the protocol prevents HTTP use.
- Restricted tokens require a referrer and may fail under `noreferrer`, `same-origin`, or privacy-blocking behavior. Verify the application's `Referrer-Policy` before production rollout.
- A production token must never authorize the broad hosting-provider domain `vercel.app`, because a domain-only rule would authorize its subdomains. Use a stable venture-owned staging hostname, a separately restricted local token, or a deliberately temporary non-production token.
- Keep the shared token until every proven consumer has a verified replacement or an intentionally tested fallback. Retire it only after configuration searches and provider usage show zero use.

See `MAPBOX_TOKEN_SEPARATION_PLAN.md` for the execution and rollback sequence.
